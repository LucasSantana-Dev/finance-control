package com.finance_control.open_finance.model.enums;

/**
 * Enumeration for account synchronization status values.
 */
public enum SyncStatus {
    PENDING,
    SYNCING,
    SUCCESS,
    FAILED,
    DISABLED
}
